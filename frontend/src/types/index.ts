// Re-export all types from shared module — do not duplicate here
export type {
  User,
  Profile,
  Category,
  PaymentMethod,
  Transaction,
  TransactionType,
  Expense,
  Budget,
  BudgetProgress,
  ExpenseSummary,
  CategoryBreakdown,
  ReceiptScanResult,
  UserSettings,
  ExportData,
} from "@shared/types";
