# SAVIQ Remediation Checklist

Source: `AUDIT_REPORT.md` (2026-06-02 security/correctness audit) + UI audit (2026-09-08 session).
Work top to bottom — severity order. Check off as fixed.

## CRITICAL

- [x] 1. **[VERIFIED 2026-09-08]** Mobile auth broken by design — `frontend/src/contexts/AuthContext.tsx` expected `session_token` in the response body; backend never sent it. **Found already fixed, uncommitted, in the working tree** (not new work this session): backend now sends the token via `X-Session-Token` response header for native clients (`_set_mobile_token_header` in `auth.py`, gated on `X-Client-Platform: ios|android` request header), and `AuthContext.tsx` + `api.ts` correctly read/send it. Verified live end-to-end via curl simulating a native client: register → `X-Session-Token` header present → token used as `Authorization: Bearer` on `/auth/me` → 200 with correct user. All 72 backend tests pass. **Still uncommitted — needs `git commit`.**

## HIGH

- [x] 2. **[VERIFIED 2026-09-08]** Shared-profile members denied access — local `_ensure_profile_owned` helpers ignored `profile_members`. **Found already fixed, uncommitted**, in `insights.py`, `ai.py`, `forecast.py`, `subscriptions.py`, `dashboard_metrics.py`, `weekly_digest.py`, `net_worth.py`, `savings_goals.py` — all swapped to `Depends`-style `get_accessible_profile()`, correctly threading the resolved `owner_user_id` through downstream queries. Verified live: created a real shared profile + invite + second account, accepted the invite, confirmed the invited (non-owner) member gets 200 (not 404) on `/insights/v2`, `/forecast`, `/dashboard/metrics`. Test data cleaned up. **Still uncommitted — needs `git commit`.**
- [x] 3. **[FIXED + VERIFIED 2026-09-08]** Invite-accept email bypass — `backend/routers/invites.py` `accept_invite_authenticated`: when `caller_email` was falsy (Apple Sign-In users hiding email), the ownership check was skipped entirely. Fixed: falls back to comparing `member_doc["invited_user_id"]` against the caller, blocking (403) when it doesn't match. Verified live with a real exploit attempt (no-email attacker user minted via direct session insert, tried to accept an invite addressed to someone else) → correctly blocked with 403. Also verified the legitimate no-email path (invite's `invited_user_id` pre-set to the real invitee at invite-creation time) still succeeds → 200. Backend imports clean, 72/72 tests pass. **Still uncommitted.**
- [x] 4. **[FIXED + VERIFIED 2026-09-08]** `SAVIQDashboard.jsx` dead demo page — deleted (1009 lines), removed `/preview` route + import from `App.jsx`, uninstalled `three`/`@react-three/fiber`/`@react-three/drei` (71 packages pruned, confirmed unused elsewhere via grep first). Verified: `/preview` now redirects to `/` via wildcard route, no console/build errors, app boots clean.
- [x] 5. **[FIXED + VERIFIED 2026-09-08]** `_month_start()`/`_add_months()`/`_parse_date_range()` duplicated across `analytics.py`, `insights.py`, `forecast_service.py`, `insights_v2_service.py` (+ inline in `ai.py`). Created `backend/utils/date_helpers.py` (also folded in `days_in_month()`, overlapping with item #17). All 5 files now import via `from utils.date_helpers import X as _X` (zero call-site changes needed — every existing `_month_start(...)` call keeps working). Also fixed `dashboard_metrics.py`'s duplicate `_days_in_month` and `ai.py`'s inline hand-rolled last-month calc (replaced with `add_months(month_start(now), -1)`). Verified: all imports clean, 72/72 tests pass, live-tested `/analytics/monthly-trend` (7 correct sequential months Mar→Sep), `/forecast`, `/dashboard/metrics`, `/insights` all 200 with correct month bucketing. **Still uncommitted.**
- [x] 6. **[INVESTIGATED 2026-09-08 — original fix rejected, would've been a regression]** Missing `user_id` guard in `get_expenses` (`expenses.py`) when `profile_id` supplied. Tried adding `user_id: profile["user_id"]` to the query as the audit suggested — then checked `create_expense` and found each expense's `user_id` is the _creating member's own id_, not the profile owner's. In a shared profile, member A's and member B's expenses coexist under one `profile_id` with different `user_id` values by design. Live-tested: had a member create an expense, confirmed the owner correctly sees it via `profile_id`-only scoping; the `user_id`-guarded query would've hidden it. **No code change made** — `get_accessible_profile()` is the correct and sole authorization boundary here; `profile_id`-only scoping is required, not a bug. Reverted the attempted change.

## MEDIUM

- [x] 7. **[FIXED + VERIFIED 2026-09-08]** Savings goal push notification read `updated.get("name", ...)` but model field is `title` — always fell back to "Your goal". Fixed `savings_goals.py:208`. Verified live: created a goal titled "My Vacation Fund", crossed the 25% milestone via a `current_amount` update, confirmed the resulting `db.notifications` doc reads `"My Vacation Fund is 25% complete!"` instead of the old generic text. 72/72 tests pass. **Still uncommitted.**
- [x] 8. **[FIXED + VERIFIED 2026-09-08]** `ThemeContext.jsx` read `localStorage.getItem("session_token")` (a key web auth never writes) to gate the settings API call — always false, dark mode never persisted server-side. Fixed: swapped provider order in `App.jsx` (`AuthProvider` now wraps `ThemeProvider`, was the reverse) so `ThemeContext` can call `useAuth()` and gate on real `user`/`isGuest` state. Verified live: `GET /api/settings` now actually fires (previously never did), toggling dark mode fires `PUT /api/settings` → 200, and the setting survives a full page reload (confirmed dark mode restored from backend, not just localStorage). Reset back to light mode after testing. **Still uncommitted.**
- [x] 9. **[INVESTIGATED 2026-09-08 — already safe, stale audit finding, no code change]** `SavingsGoalUpdate` allows `profile_id` to be changed. Checked `update_savings_goal` in `savings_goals.py` (line 199-200, already present at HEAD before this session, not new work): it resolves `target_profile_id` and calls `get_accessible_profile(target_profile_id, current_user)` before applying the update, so the caller must already have legitimate access (owner or accepted member) to the destination profile. Live-tested the exact exploit the audit described — user A tried moving their goal into user B's profile (no shared access) → correctly blocked with 403. No vulnerability present, no change needed.
- [x] 10. **[FIXED + VERIFIED 2026-09-08]** `GET /invite/accept` and `POST /invite/accept` had no rate limit. Added `@limiter.limit("20/minute")` + `request: Request` param to both. Verified live: hammered `GET /invite/accept` 22× — first 20 returned normally (404, invalid token), 21st and 22nd correctly returned 429. Confirmed `POST /invite/accept` still handles requests normally (401 unauthenticated) — decorator didn't break routing. 72/72 tests pass. **Still uncommitted.**
- [x] 11. **[FIXED + VERIFIED 2026-09-08]** `/insights/v2` endpoint path + `"schema_version": "v2"` in response body. Renamed route to `/insights/spend-comparison` (`insights.py`), dropped `schema_version` from `insights_v2_service.py`. Updated every caller found via repo-wide grep: `web/src/services/api.js` (`getV2` → `getSpendComparison`), `SmartInsightsCard.jsx` call site, `tests/backend/test_api_backend.py` (5 path refs + 1 assertion), `tests/backend/test_insights_v2_service.py` (1 assertion), and `tests/e2e/smoke.spec.ts` (route mock path + 2 `schema_version` mock fields). 72/72 backend tests pass. Verified live in browser: `GET /api/insights/spend-comparison` fires and returns 200, Smart Insights card renders correctly. **Still uncommitted.**
- [x] 12. **[FIXED + VERIFIED 2026-09-08]** Dead backend code removed: `UserSettings` model, `key_from_url()` (r2_service.py), `JWT_SECRET` config field + its two startup-validation checks in `config.py`, unused `ProfileMemberInfo`/`ProfileMemberResponse`/`ProfileMemberRole` imports in `profiles.py`. Confirmed zero usages of each via grep before deleting. `JWT_SECRET` still appears in `.env`/`.env.example` (harmless unused env var, left alone — those files aren't code). All imports clean, 72/72 tests pass, live server reloaded and responds correctly (401 on unauthenticated `/auth/me`, as expected). **Still uncommitted.**
- [x] 13. **[FIXED + VERIFIED 2026-09-08]** Dead mobile components deleted: `ExpenseCard.tsx` (180 lines), `ProfileToggle.tsx` (79 lines), `utils/format.ts` (10 lines, re-export shim whose only consumer was the also-dead `ExpenseCard.tsx`). Confirmed zero real-code imports of each via grep first (only doc-file mentions, not actual imports). Verified: `npx tsc --noEmit` clean (zero errors), Expo dev server bundles and renders the app correctly with no module-resolution errors.
- [x] 14. **[FIXED + VERIFIED 2026-09-08]** Frontend duplication extracted, all 4 sub-patterns, full rigor per user request:
  - `useIsMounted()` hook (`web/src/hooks/useIsMounted.js`) — wired into TransactionsPage, BudgetsPage, GoalsPage, NetWorthPage, AnalyticsPage. DashboardPage already had its own tested lifecycle helper (`createSmartMetricsMountedLifecycle`) — correctly left alone, not a duplicate of this pattern.
  - `<ProfileSelector />` (`web/src/components/ProfileSelector.jsx`) — wired into BudgetsPage, GoalsPage, NetWorthPage, AnalyticsPage, ExportPage (via `testId`/`responsive` props to preserve each page's distinct `data-testid` and width behavior). DashboardPage's profile switcher is a materially different, richer component (shared-member avatar pills, custom dropdown) — correctly left alone. Live-verified: switched profile via the new dropdown on Budgets, confirmed `GET /budgets/progress` re-fetched with the new `profile_id`.
  - `<DeleteConfirmModal />` (`web/src/components/DeleteConfirmModal.jsx`) — wired into TransactionsPage, BudgetsPage, SettingsPage (generic profile/category/payment delete), and BillsPage (replacing its own hand-rolled dialog — the shared version is now used everywhere, and BillsPage gains backdrop-click/Escape-key support it didn't have before). Live create→delete round-trip tested on Bills, Budgets (×2: total + category), Transactions, Settings/Categories — all confirmed working end-to-end against the real backend, test data cleaned up.
  - `useDateRangeFilter()` hook (`web/src/hooks/useDateRangeFilter.js`) — wired into DashboardPage and TransactionsPage. Live-verified the custom-range validation message still appears correctly on both pages after extraction.
  - **Bonus find while testing:** `TransactionsPage.jsx`'s "Add Transaction" modal instance was hardcoded `mode="edit"` (copy-paste bug) — title read "Edit Transaction" when adding a new transaction. Fixed to `mode="create"`, removed the stray `initialTransaction={editingTx}` prop. Verified live.
  - 116/116 web unit tests pass throughout. **Still uncommitted.**
- [x] 15. **[FIXED + VERIFIED 2026-09-08]** Dead utility exports removed after confirming zero real usages via grep for each: 9 in `web/src/lib/utils.js` (`formatTime`, `calculatePercentage`, `getInitials`, `groupByDate`, `SHARED_CATEGORY_ICON_MAP`, `CATEGORY_COLORS`, `PAYMENT_TYPES`, `TRANSACTION_COLORS`, `THEME` — all were pure re-export pass-throughs, kept `formatCurrency`/`formatDate`/`formatShortDate` which are used), 7 in `shared/utils/index.ts` (`formatTimeFromNumbers`, `getDateRangeForPeriod`, `getMonthStart`, `getWeekStart`, `clamp`, `debounce`, `isValidEmail` — kept `calculatePercentage`/`groupByDate`/`getInitials`/`generateId`, still used by mobile), 3 in `shared/constants/index.ts` (`BUDGET_PERIODS`, `RECURRING_FREQUENCIES`, `APP_CONFIG`), 2 in mobile `ThemeContext.tsx` (removed only the trailing `export { lightColors, darkColors }` — the consts themselves stay, still used internally in the same file). Verified: `npx tsc --noEmit` clean for mobile, 116/116 web unit tests pass, web app renders correctly live with no console/build errors.
- [x] 16. **[FIXED + VERIFIED 2026-09-08]** Unbounded Pydantic string fields. Added `max_length` to the 9 audit-named base-model fields (`Category.icon`=50, `Category.color`=7, `PaymentMethod.type`=30, `Expense.time`=5, `Expense.merchant`=200, `SavingsGoal.category`=100, `Notification.type`=50/`title`=200/`body`=1000). **Then discovered the real gap**: the actual API request validation goes through separate `*Create`/`*Update` models (`CategoryCreate`, `ExpenseCreate`, etc.), not the base storage models — so the base-model fix alone had near-zero practical effect. Found and fixed the same 4 fields unbounded on the request-side models too: `CategoryCreate.icon`/`CategoryUpdate.icon`, `PaymentMethodCreate.type`/`PaymentMethodUpdate.type`, `ExpenseCreate.time`/`ExpenseUpdate.time`, `SavingsGoalCreate.category`/`SavingsGoalUpdate.category` (`Expense*.merchant` already had `max_length=200`; `Notification` has no user-facing Create model, base fix is sufficient there). Verified live: POST with a 51-char category icon → 422, exactly 50 chars → 200 (created + cleaned up); 31-char payment method type → 422. 72/72 backend tests pass.
- [~] 17. **[PARTIALLY DONE 2026-09-08 — scoped down deliberately]** Backend duplication cleanup. Investigated all 4 `_to_float()` definitions first — found 3 are truly identical/compatible (`dashboard_metrics.py`, `weekly_digest_service.py`, `chat_prompt_service.py`) but the 4th (`week13_metrics_service.py`) has different, load-bearing semantics: it returns `None` (not `0.0`) for invalid/missing input, which callers use to distinguish "no data" from "actually zero" — merging it would silently change behavior. Created `backend/utils/finance.py` with `to_float(value, default=0.0)`, consolidated the 3 safe ones via `from utils.finance import to_float as _to_float` (zero call-site changes), left `week13_metrics_service.py`'s nullable version untouched and undocumented-as-duplicate. Verified: imports clean, 72/72 tests pass, live-tested `/dashboard/metrics` and `/weekly-digest` (both heavy `_to_float` users) — correct output.
  **Deferred, not done:** `resolve_profile_query()`, `get_category_name_map()`, `ensure_owned()`, and the CRUD update/delete boilerplate consolidation. Surveyed `get_category_name_map()`'s ~6 call sites and found each uses a _different_ fallback default string ("Unknown" vs "Uncategorized" vs none/KeyError-on-missing) — these are likely user-visible in the UI, so blindly unifying them risks a real behavior change, not just a refactor. This needs a slower, call-site-by-call-site pass to confirm/pick one canonical default (or keep them parameterized) — didn't want to force it through under time pressure. Recommend as a dedicated follow-up.

## LOW

- [x] 18. **[FIXED 2026-09-08]** `TransactionsPage.jsx` category filter showed "All Payment Methods" + payment-method options instead of categories (copy-paste bug) — two identical filters rendered. Fixed: now renders `categories` with "All Categories" label.
- [x] 19. **[FIXED 2026-09-08]** `NetWorthPage.jsx` empty-state button read "Add liabilitie" — naive `label.replace(/s$/, "")` singularization broke on "liabilities". Fixed with explicit `singular` prop on `SectionEmpty`. _(Found during UI audit, not in original security report.)_
- [x] 20. **[FIXED + VERIFIED 2026-09-08]** `AuthResponse` model unused, removed from `models.py` + its import in `auth.py`. Confirmed zero other usages via grep first. Bonus: also silenced a `PydanticDeprecatedSince20` warning tied to its old-style `class Config` (warning count in test output dropped 149→148). 72/72 tests pass.
- [x] 21. **[FIXED + VERIFIED 2026-09-08]** Dead API client functions removed after confirming zero usages via grep: `expensesAPI.getOne`, `statsAPI.getWeeklySummary`, `budgetsAPI.getAll`, `savingsGoalsAPI.getOne`, `billsAPI.fromSubscription` (`web/src/services/api.js`); `budgetsAPI.getAll`, `billsAPI.fromSubscription` (`frontend/src/services/api.ts`). Verified: `npx tsc --noEmit` clean (mobile), 116/116 web unit tests pass, web app renders correctly live.
- [x] 22. **[INVESTIGATED 2026-09-08 — documented, not removed]** Dead backend routes. Checked each against the _real_ running test suite (not just frontend usage): `GET /expenses/{id}` is actually load-bearing — actively used by 5+ assertions in `test_api_backend.py` (IDOR/ownership checks), audit's "dead" claim was wrong there, no change made. For the other 3 (`GET /expenses/{id}/attachments/{key}/url`, `GET /savings-goals/{id}`, plus a bonus find — `GET /stats/weekly-summary`, orphaned as a side-effect of #21's cleanup): each is a complete, secure, working implementation, just not yet wired to any UI action. Chose **document over remove** — deleting real, working engineering effort for near-zero benefit is worse than leaving a docstring note. Added a one-line "not currently called by web or mobile, kept as ready-to-use API surface" note to each. 72/72 tests still pass.
- [x] 23. **[FIXED + VERIFIED 2026-09-08]** 22 unused imports removed after confirming zero usages via grep for each. Backend (6, not 10 — 4 of the audit's list were already covered by #12/#20's deletions): `import os` (`deps.py`), `import asyncio` (`services/push_service.py`), `HTTPException` (`routers/push.py`), `import asyncio` + `Query` (`routers/bills.py`), `import uuid` (`routers/budgets.py`). Frontend (12): `AreaChart`/`Area` (`AnalyticsPage.jsx`), `useNavigate`/`ChevronRight` (`BillsPage.jsx` — also removed the now-unused `const navigate = useNavigate()` line and an orphaned `DELETE CONFIRM` comment header left over from #14's dialog migration), `AlertCircle`/`CheckCircle2` (`BudgetsPage.jsx`), `Input`/`Card` (`LoginPage.jsx`), `Input`/`Select` (`AddTransactionModal.jsx`), `X` (`Layout.jsx`), `invitesAPI` (mobile `app/(tabs)/more.tsx`). Verified: `npx tsc --noEmit` clean (mobile), 116/116 web unit tests pass, 72/72 backend tests pass, live-rendered `/analytics`, `/bills`, `/budgets`, `/transactions` all clean (no error overlay, correct content) via fresh DOM checks.
- [x] 24. **[FIXED + VERIFIED 2026-09-08 — 5 of 6, 1 deliberately left]** `formatCurrency` reimplemented locally in 6 files. 5 fixed: `frontend/app/(tabs)/stats.tsx`, `WeeklyDigestWidget.tsx`, `SubscriptionDetectionWidget.tsx`, `ForecastWidget.tsx` now delegate to `@shared/utils`'s canonical `formatCurrency` (kept each file's local `formatCurrency(value: unknown)` wrapper — its `Number.isFinite` NaN-guard isn't in the shared version — but the wrapper now calls the shared formatter internally instead of duplicating `Intl.NumberFormat`, zero call-site changes). `web/src/components/SubscriptionsCard.jsx`'s `formatUsd` removed entirely in favor of the `formatCurrency` already imported from `../lib/utils` everywhere else in web (that file already re-exports `@shared/utils`'s version) — call sites now guard with `|| 0` inline, matching `formatUsd`'s old `Number(value || 0)` behavior. **Deliberately left:** `frontend/src/utils/smartMetricsCards.js`'s `formatCurrencyValue` — first attempted the same fix, but its test file (`smartMetricsCards.test.mjs`) runs via plain `node --test` with no bundler, and `@shared` is a Metro/tsconfig-only path alias (`shared/utils` is TypeScript source, not a real Node-resolvable package) — the import broke the test with `ERR_MODULE_NOT_FOUND`. Reverted that one file back to its original self-contained implementation rather than force it through. Verified: `npx tsc --noEmit` clean, 116/116 web unit tests pass, 4/4 `smartMetricsCards.test.mjs` tests pass (after revert), 72/72 backend tests pass. Live-verified web: `/` dashboard's Subscriptions card renders `$0.00` via the new `formatCurrency` call, no console/build errors. Live-verified mobile: started the Expo dev server fresh, logged in as guest, confirmed the dashboard (which renders `stats.tsx`'s and all 3 widget files' `formatCurrency`) bundles and renders cleanly — only pre-existing guest-mode network errors in console, no module-resolution or bundling errors.
- [x] 25. **[FIXED + VERIFIED 2026-09-08]** Minimum password length bumped 6→8 per NIST SP 800-63B. Server-side enforcement point (the only one that matters — confirmed it's the sole real gate): `validate_password()` in `backend/deps.py:42-44`, called from the email-register endpoint (`auth.py:372`) only, not login (existing users with shorter passwords stay able to log in — correct, this is a new-password-creation policy, not retroactive). Also updated matching client-side copy/pre-checks (which only affect UX, not security) in `web/src/pages/LoginPage.jsx` (validation guard + placeholder) and `frontend/app/index.tsx` (validation guard + placeholder). Verified test passwords already ≥8 chars (`tests/backend/test_api_backend.py` uses `"secret123"`), so no test breakage. 72/72 backend tests, 116/116 web tests, `tsc --noEmit` all pass. Live-verified via curl against the real running server: 7-char password → `400 Password must be at least 8 characters`; 8-char password → `200` (registered, then cleaned up via mongosh).
- [x] 26. **[FIXED + VERIFIED 2026-09-08]** `passlib==1.7.4` (unmaintained since 2023, throws an internal `AttributeError` compat warning against modern `bcrypt` versions — reproduced live below) migrated to calling the already-pinned `bcrypt==4.1.3` package directly. `backend/deps.py`: replaced `CryptContext(schemes=["bcrypt"])` with `bcrypt.hashpw(password.encode(), bcrypt.gensalt())` / `bcrypt.checkpw(...)`. Removed `passlib==1.7.4` from `requirements.txt` and uninstalled the package (confirmed nothing else depended on it via `pip show -f`). Single call site (`auth.py:448`) unaffected — same `bool`-returning signature. **Backward-compatibility was the real risk here** (existing users' hashes were created by passlib) — verified empirically: temporarily reinstalled passlib, generated a hash with the _old_ code path, confirmed the _new_ `bcrypt.checkpw()` correctly verifies it (`True`) and correctly rejects a wrong password against it (`True`), then uninstalled passlib again. Also live-verified end-to-end against the real running server: registered a new user (hash created by the new code, confirmed `$2b$12$` format — identical to passlib's), logged in with the correct password → 200, wrong password → 401. 72/72 backend tests pass. Test user cleaned up via mongosh.
- [x] 27. **[INVESTIGATED 2026-09-08 — false positive, no code change]** `print(json.dumps(summary, ...))` in `backend/scripts/check_integrity.py:16`. The audit's "no `print()` in backend" rule is written for the live FastAPI app (`routers/`, `services/`) where `print()` bypasses structured logging — this file isn't that. It's a standalone CLI maintenance script (`if __name__ == "__main__": asyncio.run(main())`), and the `print()` call _is_ its output contract: it emits the integrity-check summary as machine-readable JSON to stdout so an operator can pipe/redirect it (e.g. `python check_integrity.py > report.json`). Swapping it for `logger.info()` would actively break that — the logging formatter (`"%(asctime)s %(levelname)s %(message)s"`, configured 2 lines above) would prepend a timestamp/level to every line and corrupt the JSON. Confirmed this is the established convention for this directory, not an oversight: the sibling script `backend/scripts/seed_savings_goals.py:43` does the same (`print(result)` for its own CLI output). No change made.
- [x] 28. **[FIXED + VERIFIED 2026-09-08]** Misleading comment `# R2 public URLs` on `Expense.attachments` (`models.py:100`). Confirmed the real behavior via `routers/expenses.py`: the upload endpoint pushes `object_key` (an R2 key, not a URL) into the array (`"$push": {"attachments": object_key}`), and there's a dedicated `GET /expenses/{id}/attachments/{key}/url` endpoint that calls `generate_presigned_url(key)` — i.e. these are private object keys requiring a signed URL to access, never public URLs. Fixed comment to `# R2 object keys (private; access via presigned URL endpoint)`. 72/72 backend tests pass (comment-only change).
- [x] 29. **[FIXED + VERIFIED 2026-09-08]** Dead CSS `.animate-slide-up` + its `@keyframes slideUp` (`web/src/index.css:74-87`) — confirmed zero usages anywhere via grep (`.animate-fadeIn`'s sibling rule right above it is still used elsewhere, left alone). Removed both. Verified live: web app renders clean, no console/build errors.

## Product / UX gaps (from UI audit, not in original security report)

- [x] 30. **[CORRECTED 2026-09-08 — original audit claim was wrong]** Claimed categories/payment methods can't be edited/deleted. Found while testing #14: custom (non-default) categories DO have working hover-revealed "Edit category"/"Delete category" buttons — verified live (created + deleted a test category through the real UI). Only the 9 seed _default_ categories lack edit/delete, which is reasonable (they're the app's baseline set). No gap — no action needed.
- [x] 31. **[FIXED + VERIFIED 2026-09-08]** Delete flows were inconsistent. `<DeleteConfirmModal />` (built for #14) unified Transactions/Budgets/Settings(category,payment,profile)/Bills/Goals — all 6 delete surfaces now share one component. Migrated Goals off `window.confirm("Delete this goal?")` last (finishing what #14 started). Delete Account intentionally stays separate (needs typed "DELETE" confirmation, different UX by design — not a target for this unification). Verified live: created a goal, triggered delete, confirmed the real modal (not a native browser dialog) appears with correct title text, confirmed deletion completes. 116/116 web tests pass.
- [x] 32. **[VERIFIED 2026-09-08 — user approved triggering downloads]** Export buttons checked (JSON turned out not to be a separate download button — it's the endpoint the on-screen "Export Preview" already fetches; the two actual download buttons are CSV and PDF). Live-clicked both real buttons in the browser (Sid's account, empty date range): `GET /export/csv` and `GET /export/json` both 200, CSV returned a correct empty-body header row, PDF's `handleExportPDF` (client-side, via `jsPDF`) ran with zero console errors. For real content, rather than adding noise to Sid's own data, used a throwaway test account with one real expense + one real income transaction: `GET /export/csv` returned correctly formatted rows with resolved category/payment-method names (not raw IDs) and correct amounts; `GET /export/json` returned correct `summary` math (income 2500.0 − expense 45.5 = balance 2454.5) and correct `category_breakdown`. Read `ExportPage.jsx`'s PDF-building code and confirmed it sources its numbers from this same, now-verified-correct JSON payload (`data.summary.*`, `data.category_breakdown`), so the already-passing live PDF click plus this verified data source together confirm the PDF's content is correct too. Test account and data cleaned up via mongosh.

## New findings (surfaced while verifying other items)

- [x] 33. **[FIXED + VERIFIED 2026-09-08]** `users.email` unique-index collision for no-email Apple Sign-In users. Root cause confirmed in `routers/auth.py:345` (`apple_auth_login`'s new-user path writes literal `"email": token_email` where `token_email` can be `""`) combined with `database.py:35`'s index being `unique=True, sparse=True` — **`sparse` only exempts a genuinely _missing_ field, not an empty string that's present**, so a second no-email Apple user would 500 on a duplicate-key error. Considered changing the write side to store `None` instead of `""`, but `models.py`'s `User.email: str` is required (non-`Optional`) and `GET /auth/me` declares `response_model=User`, so a `None` email would 500 on every subsequent `/auth/me` call for that user — worse than the original bug. Fixed at the index instead (lower-risk, matches the codebase's own existing convention — `database.py` already uses this exact `partialFilterExpression` pattern for `name_normalized` fields on 3 other collections): replaced the sparse unique index with `unique=True, partialFilterExpression={"email": {"$type": "string", "$gt": ""}}` (drops+recreates the old `email_1` index defensively on startup, since Mongo won't let two conflicting index definitions coexist under the same name). Verified live against the real dev DB: inserted two users with `email: ""` directly — both succeeded (previously the second would 500); confirmed uniqueness is still enforced for genuine duplicate real emails (`E11000 duplicate key error`). Test data cleaned up. 72/72 backend tests pass.
- [x] 34. **[FIXED + VERIFIED 2026-09-08 — scope expanded well past the original one-file estimate, per explicit user direction]** Started as "`test_savings_goals_api.py` is stale, fix or delete it." User clarified the real ask: `pytest.ini` (`python_files = test_api_backend.py`) was silently excluding **19 other test files** in `tests/backend/` from the real suite, not just this one. Widening the pattern to `test_*.py` and running all 20 files surfaced **45 failures across 7 files** — far bigger than the original item. Triaged and fixed all of them:
  - **34 failures, 6 files — mechanical, same root cause as the original finding**: `test_ai_chat_insights_api.py`, `test_dashboard_metrics_api.py`, `test_openai_migration.py`, `test_savings_goals_api.py`, `test_subscriptions_summary_api.py`, `test_weekly_digest_api.py` all had a local `_register()` helper that returned `response.json()` directly — pre-M9 that included `session_token`, post-M9 it doesn't (it's a cookie now). Fixed all 6 identically: extract `response.cookies.get("session_token")` and inject it into the returned dict.
  - **3 failures, 1 file — missing router in a shared test fixture**: `test_ai_chat_insights_api.py` still had 429s after the above. Root cause: `tests/backend/conftest.py`'s `client` fixture disables the rate limiter for `main`/`auth`/`expenses`/`invites` (each router owns its own separate `Limiter` instance) but never included `ai.py`'s — an oversight from whenever `ai.py`'s own limiter was added, unrelated to auth. Added `ai` to the fixture's disable/restore list. This is a real fixture bug fix, benefits every test in every file that touches a rate-limited AI endpoint, not just this one.
  - **2 stale assertions, expected old behavior**: `test_chat_insights_endpoint_blocks_wrong_profile_access` and `test_subscriptions_summary_wrong_profile_access_blocked` both expected `404` for "profile exists but you don't have access" — stale from before #2's session-wide migration to `get_accessible_profile()`, which correctly returns `403` for that case (404 is reserved for "profile doesn't exist"). Updated both to expect `403`.
  - **1 stale assertion, wrong response shape**: `test_chat_insights_failure_logs_non_sensitive_diagnostics` asserted `response.json()["detail"]`, but this app wraps all HTTP errors in a custom `{"error": {"code", "message"}}` envelope (`errors.py`) — there's no top-level `detail` key. Fixed to `response.json()["error"]["message"]`, matching the convention already used throughout `test_api_backend.py`.
  - **1 real production bug found and fixed**: `test_insights_v2_service.py`'s category-comparison tests expected real category names but got `"uncategorized"` for everything. Root cause: `services/insights_v2_service.py`'s `_PROJECTION = {"_id": 0, "amount": 1, "type": 1}` (used on every Mongo query in `generate_spend_comparison`, which backs the live `/api/insights/spend-comparison` endpoint) never included `category_id` — so `_category_totals()`'s `expense.get("category_id")` was always `None`, always falling back to `"uncategorized"`. **This is a live bug affecting real users**, not a stale test — every category breakdown on the Spend Comparison feature has always reported everything as uncategorized regardless of actual category. Fixed: added `"category_id": 1` to `_PROJECTION`. Verified live against the real running server: created a real categorized expense, confirmed `GET /api/insights/spend-comparison` now returns the real `category_id` instead of `"uncategorized"`. Test data cleaned up.
  - **2 miscalibrated test fixtures**: `test_severity_mapping_coverage_all_levels`'s "warning" and "high" cases used spend/day values that, when run through the real risk-scoring math (`_build_budget_risk` in `insights_v2_service.py`), actually landed in different severity tiers than the test's own labels claimed (verified by hand-computing the formula) — not an app bug, just fixture values that didn't produce the scenario they were meant to test. Recalibrated both to values that genuinely land in their intended tier (confirmed by the same hand computation), leaving the app's scoring logic untouched.
  - **1 malformed test URL**: `test_dashboard_metrics_additive_route_keeps_existing_analytics_payload_compatible` built a query string with a literal unencoded `+00:00` in an ISO timestamp — in a URL query string, an unencoded `+` decodes to a space, so `datetime.fromisoformat()` server-side correctly rejected it with 400. Verified this exact mechanism live via curl (raw `+00:00` → 400, `%2B00:00` → 200) before concluding it was a test bug, not an app bug. Fixed by percent-encoding it.
  - **1 stale test date**: `test_dashboard_metrics_mixed_partial_data_case_returns_consistent_safe_mix` hardcoded an expense date of `2026-04-05`, but `dashboard/metrics` scopes expenses to the current calendar month relative to real wall-clock time (now September, not April) — the expense fell outside the query window, so `spend_velocity` legitimately had no data to work with. Fixed by using `datetime.now(timezone.utc)` instead of a fixed date.
  - **1 missing required field**: `test_dashboard_metrics_valid_scoped_request_returns_expected_bundle`'s `_create_goal()` helper omitted `deadline` and `category`, both required (non-`Optional`) fields on `SavingsGoalCreate` — always a 422, unrelated to any of this session's earlier changes. Added both.
  - **2 design-question assertions, resolved with the user**: `test_dashboard_metrics_no_data_scenario_returns_safe_explicit_structures` expected `has_sufficient_data: False` for a brand-new zero-data profile on both `budget_confidence` and `projected_savings_summary`. Traced the actual logic (`week13_metrics_service.py`): "no budget set" and "zero net position / zero spend" are both real, present values (not missing data) that the service correctly and deliberately treats as sufficient to compute a real answer ("no budget → low risk"; "$0 projected savings"). Asked the user to confirm this was intentional design rather than a bug before touching either the test or the app — confirmed intentional. Updated both assertions to expect `True`, matching the deliberate design.
  - **pytest.ini widened**: `python_files` changed from `test_api_backend.py` to `test_*.py`, per explicit user request, so all 20 files (179 tests) now run by default instead of 72. Final run: **179/179 pass.**

---

**Progress: 34/34 done, #17 and #24 partial** (2 UI bugs, #1-#34 complete except #17/#24 partial, plus 3 bonus finds fixed — including a real production bug in the live Spend Comparison feature). #6, #9, #27 closed as investigated/already-safe/false-positive with no code change. #17's `to_float` consolidation done; broader dedup deferred (real per-call-site differences, needs a slower pass). #22 documented rather than removed (working code, just not yet wired to UI). #24 done 5/6 (6th file's test harness can't resolve the shared TS package without a bundler). Backend test suite widened from 72 to 179 tests (pytest.ini). **Every item on the checklist has now been addressed. Nothing has been committed yet** — the working tree has a large uncommitted diff across backend, web, and mobile.
