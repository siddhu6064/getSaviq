name: CI

on:
  push:
  pull_request:

jobs:
  backend-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install system dependencies
        run: sudo apt-get update -qq && sudo apt-get install -y libmagic1

      - name: Install backend test dependencies
        run: pip install -r backend/requirements-dev.txt

      - name: Run supported backend pytest suite
        run: pytest tests/backend/test_api_backend.py -q

  playwright-smoke:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up Node
        uses: actions/setup-node@v5
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: web/package-lock.json

      - name: Install Playwright test dependency
        run: npm install

      - name: Install web dependencies
        run: npm --prefix web ci

      - name: Build web app
        run: npm --prefix web run build

      - name: Install Playwright browser
        run: npx playwright install --with-deps chromium

      - name: Run smoke suite
        run: npx playwright test tests/e2e/smoke.spec.ts --config=playwright.config.ts
